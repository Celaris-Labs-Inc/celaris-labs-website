# Celaris Labs

Static company site for [celarislabs.org](https://celarislabs.org).

## Local

Open `index.html` in a browser, or serve the folder:

```bash
python3 -m http.server 4173
```

## Deploy

Cloudflare Pages builds from this repository’s root. The custom domain `celarislabs.org` is attached to the Pages project.
